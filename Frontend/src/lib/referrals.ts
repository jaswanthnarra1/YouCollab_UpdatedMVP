/**
 * Display-only mirror of Backend/src/utils/referrals.js — keep in sync
 * manually, no shared package between Frontend/Backend in this repo (same
 * pattern as credits.ts / credits.js). The frontend must NEVER compute
 * eligibility itself; it only shows what the server already decided.
 */
export const REFERRAL_VIEW_THRESHOLD = 50000;

export const REFERRAL_REWARD_AMOUNT = 1000;
